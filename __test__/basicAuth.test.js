const basicAuth = require('../src/auth/middleware/basicAuth.js')

describe('testing basicAuth functions', () => {

  test('valid basic auth credentials', () => {
    // let req = {};
    // let res = {};
    // let next = ()=>{};
    // timeStamp(req, res, next);
    // expect(req.requestTime).toBeTruthy();
  })

  test('invalid credentials', () => {
    // let req = {
    //   requestTime: "request time",
    //   method: "method",
    //   path: "path",
    // };
    // let res = {};
    // let next = ()=>{};
    // logger(req, res, next);
    // expect(console.log).toHaveBeenLastCalledWith("request time", "method", "path");
  })
})